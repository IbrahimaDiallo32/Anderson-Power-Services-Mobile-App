CREATE TABLE IF NOT EXISTS "test_table"(
                                           "id" int NOT NULL,
                                           "title" text,
                                           CONSTRAINT "book_id_pk" PRIMARY KEY ("id")
);